$("#new_post").before('<div id="flash_notice"><=%raw escape_javascript(flash.delete(:notice)) %></div>');
$("#latest-posts ul li").prepend("<%=raw escape_javascript(render(@post)) %>");
$("#new_post")[0].reset();
